import React from 'react';\n\nconst DifficultyBadge: React.FC<{ level: string }> = ({ level }) => {\n    let color;\n    switch (level) {\n        case 'easy':\n            color = 'green';\n            break;\n        case 'medium':\n            color = 'yellow';\n            break;\n        case 'hard':\n            color = 'red';\n            break;\n        default: \n            color = 'gray';\n    }\n\n    return (\n        <span style={{ color }}>{level.charAt(0).toUpperCase() + level.slice(1)}</span>\n    );\n};\n\nexport default DifficultyBadge;